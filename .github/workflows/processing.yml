name: CI (processing)

on:
  push:
    branches: [main]
    paths:
      - "processing/**"
      - "infra/k8s/charts/wiki-processing/**"
      - ".github/workflows/processing-ci.yml"
      - "pyproject.toml"
      - "uv.lock"
      - "Makefile"

  pull_request:
    branches: [main]
    paths:
      - "processing/**"
      - "infra/k8s/charts/wiki-processing/**"
      - ".github/workflows/processing-ci.yml"
      - "pyproject.toml"
      - "uv.lock"
      - "Makefile"

permissions:
  contents: read
  id-token: write

concurrency:
  group: processing-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: Lint (mypy/black/ruff)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v3
        with:
          enable-cache: true

      - name: Sync dependencies
        run: uv sync --group processing --group dev --frozen

      - name: Cache uv
        uses: actions/cache@v4
        with:
          path: ~/.cache/uv
          key: ${{ runner.os }}-uv-${{ hashFiles('**/uv.lock') }}
          restore-keys: |
            ${{ runner.os }}-uv-

      - name: ty
        run: make ty app=processing

      - name: black (check)
        run: make black app=processing

      - name: ruff
        run: make ruff app=processing

  tests:
    name: Tests
    runs-on: ubuntu-latest
    needs: lint

    strategy:
      fail-fast: false
      matrix:
        marker: [unit, integration]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v3
        with:
          enable-cache: true

      - name: Sync dependencies
        run: uv sync --group processing --group dev --frozen

      - name: Run pytest (-m ${{ matrix.marker }})
        run: uv run pytest ./processing/tests/ -m ${{ matrix.marker }} -q

  build:
    name: Build image
    environment: prod
    needs: [lint, tests]
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - id: auth
        uses: google-github-actions/auth@v3
        with:
          project_id: ${{ secrets.GCP_PROJECT_ID }}
          workload_identity_provider: ${{ secrets.WIF_PROVIDER }}
          service_account: ${{ secrets.CI_SA_EMAIL }}

      - uses: google-github-actions/setup-gcloud@v2

      - name: Configure docker auth
        run: |
          gcloud auth configure-docker europe-west3-docker.pkg.dev --quiet

      - name: Build & Push
        env:
          PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
          IMAGE: wiki-processing
          TAG: ${{ github.sha }}
          STAGING_BUCKET: ${{ secrets.STAGING_BUCKET }}
        run: |
          gcloud builds submit \
            --config processing/cloudbuild.yaml \
            --gcs-source-staging-dir "gs://${STAGING_BUCKET}/source" \
            --substitutions=_TAG=$GITHUB_SHA,_IMAGE=$IMAGE \
            .

  deploy:
    name: Deploy
    environment: prod
    needs: [build]
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - id: auth
        uses: google-github-actions/auth@v3
        with:
          project_id: ${{ secrets.GCP_PROJECT_ID }}
          workload_identity_provider: ${{ secrets.WIF_PROVIDER }}
          service_account: ${{ secrets.CI_SA_EMAIL }}

      - uses: google-github-actions/setup-gcloud@v2
        with:
          install_components: "gke-gcloud-auth-plugin"

      - name: Get GKE credentials
        env:
          PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
          REGION: ${{ secrets.REGION }}
          GKE_CLUSTER: ${{ secrets.GKE_CLUSTER }}
          USE_GKE_GCLOUD_AUTH_PLUGIN: "True"
        run: |
          set -eux
          gcloud container clusters get-credentials "$GKE_CLUSTER" \
            --region "$REGION" \
            --project "$PROJECT_ID"

      - name: Setup Helm
        uses: azure/setup-helm@v4
        with:
          version: v3.14.4

      - name: Deploy processing with Helm
        env:
          PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
          NAMESPACE: ${{ secrets.HELM_NAMESPACE }}
          IMAGE_REPO: europe-west3-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/wiki-stream-analytics/wiki-processing
          IMAGE_TAG: ${{ github.sha }}
          HELM_KUBERNETES_QPS: "50"
          HELM_KUBERNETES_BURST: "100"
        run: |
          set -eux

          helm dependency update infra/k8s/charts/wiki-processing
          helm dependency build infra/k8s/charts/wiki-processing

          helm upgrade --install wiki-processing infra/k8s/charts/wiki-processing \
            --set serviceAccount.create=false \
            --set serviceAccount.name=processing-sa \
            --namespace "$NAMESPACE" \
            --set image.repository="$IMAGE_REPO" \
            --set image.tag="$IMAGE_TAG" \
            --set gcp.projectId="${PROJECT_ID}" \
            --timeout 15m --debug

          kubectl -n "$NAMESPACE" rollout status deploy/wiki-processing-wiki-processing --timeout=300s